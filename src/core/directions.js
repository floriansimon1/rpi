"use strict";

const Directions = {
    UP:    Symbol("UP"),
    DOWN:  Symbol("DOWN"),
    LEFT:  Symbol("LEFT"),
    RIGHT: Symbol("RIGHT"),
    STILL: Symbol("STILL")
};

module.exports = Directions;
